// DEVELOPMENT overrides (swapped in via fileReplacements during `ng serve`).
// Points the frontend at your real backend running on :5005.
export const environment = {
  production: false,
  apiBase: 'http://localhost:5005',
};
